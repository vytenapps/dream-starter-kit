/**
 * Client-safe barrel — react-query hooks shared by web + native.
 */
export {
  useChatThreads,
  useCreateThread,
  useDeleteThread,
  useSendMessage,
  useThreadMessages,
} from "./hooks/use-chat";
