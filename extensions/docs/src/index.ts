/**
 * Client barrel for @acme/ext-docs. The docs UI is server-rendered (./web-server)
 * with small client islands (./web); there are no shared client hooks yet, so
 * this stays empty (the host's generated registries import the entries directly).
 */
export {};
