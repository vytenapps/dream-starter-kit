/**
 * Client-safe barrel (validators + react-query hooks, shared web + native).
 * usePremium() and the billing screens move here from packages/app and the
 * apps in phase 7 of the extension refactor (docs/EXTENSIONS-PLAN.md §8).
 */
export {};
