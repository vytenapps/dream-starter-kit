/**
 * Client-safe barrel (validators + react-query hooks, shared web + native).
 * The notifications hooks move here from packages/app in phase 3 of the
 * extension refactor (docs/EXTENSIONS-PLAN.md §8).
 */
export {};
